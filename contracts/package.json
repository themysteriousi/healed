{
  "name": "ugf-contracts",
  "version": "1.0.0",
  "description": "Solidity contracts for the UGF Gasless NFT Badge demo",
  "scripts": {
    "compile": "npx hardhat compile --config hardhat.config.js",
    "deploy": "npx hardhat run scripts/deploy.js --network sepolia --config hardhat.config.js",
    "deploy:local": "npx hardhat run scripts/deploy.js --network hardhat --config hardhat.config.js",
    "verify": "npx hardhat verify --network sepolia --config hardhat.config.js"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^6.1.2",
    "dotenv": "^16.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  },
  "type": "module"
}
